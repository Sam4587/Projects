<view class="customs-container paper-cut-bg">
  <!-- 头部 -->
  <view class="header">
    <view class="header-title">地域习俗</view>
    <view class="header-subtitle">了解各地红包文化</view>
  </view>

  <!-- 内容区域 -->
  <view class="content">
    <view class="card">
      <view class="card-title">
        <text class="icon">📚</text>
        <text>地域习俗介绍</text>
      </view>
      
      <view class="form-group">
        <view class="form-label">选择省份</view>
        <view class="region-selector" bindtap="showRegionPicker">
          <text class="selector-text">
            {{regions[selectedRegionIndex] ? regions[selectedRegionIndex].name : '请选择省份'}}
          </text>
          <text class="selector-arrow">▼</text>
        </view>
      </view>

      <!-- 标签切换 - 优化为垂直布局 -->
      <view class="tab-list">
        <view
          class="tab-item {{customsTab === 'gift' ? 'active' : ''}}"
          bindtap="switchCustomsTab"
          data-tab="gift"
        >
          <text class="tab-icon">🧧</text>
          <text class="tab-text">压岁钱</text>
        </view>
        <view
          class="tab-item {{customsTab === 'giving' ? 'active' : ''}}"
          bindtap="switchCustomsTab"
          data-tab="giving"
        >
          <text class="tab-icon">💕</text>
          <text class="tab-text">随份子</text>
        </view>
        <view
          class="tab-item {{customsTab === 'special' ? 'active' : ''}}"
          bindtap="switchCustomsTab"
          data-tab="special"
        >
          <text class="tab-icon">🎉</text>
          <text class="tab-text">特殊场合</text>
        </view>
      </view>

      <!-- 压岁钱习俗内容 -->
      <block wx:if="{{customsTab === 'gift'}}">
        <view class="space-y-4">
          <view class="bg-red-50 p-3 rounded-lg">
            <view class="flex items-center justify-between mb-2">
              <text class="text-sm font-medium text-gray-600">金额区间</text>
              <text class="text-red-600">📈</text>
            </view>
              <view class="text-xl font-bold text-red-600">
                {{giftMoneyData.giftMoney.amount || '暂无数据'}}
              </view>
          </view>

          <view>
            <view class="form-label">当地习俗</view>
            <view class="space-y-2">
              <block wx:for="{{giftMoneyData.giftMoney.customs || []}}" wx:key="*this">
                <view class="flex items-start">
                  <text class="text-red-600 mr-2">•</text>
                  <text class="text-sm text-gray-600">{{item}}</text>
                </view>
              </block>
            </view>
          </view>

          <view class="bg-yellow-50 p-3 rounded-lg border border-yellow-200">
            <view class="form-label text-yellow-800">贴心提示</view>
              <text class="text-sm text-yellow-700">
                {{giftMoneyData.giftMoney.tips || '暂无提示'}}
              </text>
          </view>

          <!-- 深度解读按钮（暂时隐藏，等待正式版广告ID） -->
          <!--
          <view class="mt-4">
            <button 
              class="btn btn-secondary w-full" 
              bindtap="unlockDeepReading"
            >
              <text class="btn-icon">🎬</text>
              <text class="btn-text">
                <text wx:if="{{!unlockedDeepReadings.includes(selectedRegion)}}">观看视频，解锁深度解读</text>
                <text wx:if="{{unlockedDeepReadings.includes(selectedRegion)}}">📖 查看深度解读</text>
              </text>
            </button>
          </view>
          -->
        </view>
      </block>

      <!-- 随份子习俗内容 -->
      <block wx:if="{{customsTab === 'giving'}}">
        <view class="space-y-4">
          <view class="grid grid-cols-2 gap-4">
            <view class="bg-red-50 p-3 rounded-lg">
              <view class="flex items-center justify-between mb-2">
                <text class="text-sm font-medium text-gray-600">普通同事</text>
                <text class="text-red-600">👥</text>
              </view>
              <view class="text-lg font-bold text-red-600">
                {{giftGivingData.giving.colleague || '暂无数据'}}
              </view>
            </view>

            <view class="bg-orange-50 p-3 rounded-lg">
              <view class="flex items-center justify-between mb-2">
                <text class="text-sm font-medium text-gray-600">好朋友</text>
                <text class="text-orange-600">💕</text>
              </view>
              <view class="text-lg font-bold text-orange-600">
                {{giftGivingData.giving.friend || '暂无数据'}}
              </view>
            </view>
          </view>

          <view>
            <view class="form-label">当地习俗</view>
            <view class="space-y-2">
              <block wx:for="{{giftGivingData.giving.customs || []}}" wx:key="*this">
                <view class="flex items-start">
                  <text class="text-red-600 mr-2">•</text>
                  <text class="text-sm text-gray-600">{{item}}</text>
                </view>
              </block>
            </view>
          </view>

          <view class="bg-yellow-50 p-3 rounded-lg border border-yellow-200">
            <view class="form-label text-yellow-800">贴心提示</view>
            <text class="text-sm text-yellow-700">
              {{giftGivingData.giving.tips || '暂无提示'}}
            </text>
          </view>

          <!-- 深度解读按钮（暂时隐藏，等待正式版广告ID） -->
          <!--
          <view class="mt-4">
            <button
              class="btn btn-secondary w-full"
              bindtap="unlockDeepReading"
            >
              <text class="btn-icon">🎬</text>
              <text class="btn-text">
                <text wx:if="{{!unlockedDeepReadings.includes(selectedRegion)}}">观看视频，解锁深度解读</text>
                <text wx:if="{{unlockedDeepReadings.includes(selectedRegion)}}">📖 查看深度解读</text>
              </text>
            </button>
          </view>
          -->
        </view>
      </block>

      <!-- 特殊场合内容 - 优化为更紧凑的布局 -->
      <block wx:if="{{customsTab === 'special'}}">
        <view class="space-y-3">
          <!-- 简化：紧凑的卡片列表 -->
          <view class="occasion-compact-list">
            <!-- 婚礼习俗 -->
            <view class="occasion-compact-item" bindtap="toggleOccasionDetail" data-type="wedding">
              <text class="compact-icon">💒</text>
              <view class="compact-info">
                <text class="compact-title">婚礼</text>
                <text class="compact-amount">{{giftMoneyData.specialOccasions.wedding.amount || '暂无数据'}}</text>
              </view>
              <text class="compact-arrow">></text>
            </view>
            <!-- 生日习俗 -->
            <view class="occasion-compact-item" bindtap="toggleOccasionDetail" data-type="birthday">
              <text class="compact-icon">🎂</text>
              <view class="compact-info">
                <text class="compact-title">生日</text>
                <text class="compact-amount">{{giftMoneyData.specialOccasions.birthday.amount || '暂无数据'}}</text>
              </view>
              <text class="compact-arrow">></text>
            </view>
            <!-- 葬礼习俗 -->
            <view class="occasion-compact-item" bindtap="toggleOccasionDetail" data-type="funeral">
              <text class="compact-icon">⚰️</text>
              <view class="compact-info">
                <text class="compact-title">葬礼</text>
                <text class="compact-amount">{{giftMoneyData.specialOccasions.funeral.amount || '暂无数据'}}</text>
              </view>
              <text class="compact-arrow">></text>
            </view>
          </view>

          <!-- 展开式详情（点击后显示） -->
          <view wx:if="{{expandedOccasion}}" class="occasion-detail-section">
            <view class="detail-card">
              <view class="detail-header">
                <text class="detail-title">{{expandedOccasion.title}}</text>
                <text class="detail-close" bindtap="toggleOccasionDetail">✕</text>
              </view>
              <view class="detail-content">
                <view class="detail-section">
                  <view class="detail-label">当地习俗</view>
                  <block wx:for="{{expandedOccasion.customs}}" wx:key="*this">
                    <view class="detail-item">• {{item}}</view>
                  </block>
                </view>
                <view class="detail-tip-box">
                  <view class="detail-tip-label">贴心提示</view>
                  <text class="detail-tip-text">{{expandedOccasion.tips}}</text>
                </view>
              </view>
            </view>
          </view>

          <!-- 禁忌文化 -->
          <view class="taboo-section">
            <view class="taboo-header">
              <text class="taboo-icon">⚠️</text>
              <text class="taboo-title">禁忌文化</text>
            </view>
            <view class="taboo-content">
              <block wx:for="{{giftMoneyData.taboo || []}}" wx:key="*this">
                <view class="taboo-item">{{item}}</view>
              </block>
            </view>
          </view>

          <!-- 文化故事 -->
          <view class="story-section">
            <view class="story-header">
              <text class="story-icon">📖</text>
              <text class="story-title">文化故事</text>
            </view>
            <view class="story-content">
              <text class="story-text">{{giftMoneyData.story || '暂无文化故事'}}</text>
            </view>
          </view>
        </view>
      </block>
    </view>
  </view>

  <!-- 地区选择弹窗 -->
  <view wx:if="{{showRegionPicker}}" class="modal-overlay" bindtap="hideRegionPicker">
    <view class="modal-content picker-modal" catchtap="">
      <view class="modal-header">
        <text class="modal-title">选择省份</text>
        <text class="modal-close" bindtap="hideRegionPicker">✕</text>
      </view>

      <scroll-view class="picker-list" scroll-y="true">
        <view
          wx:for="{{regions}}"
          wx:key="id"
          class="picker-item {{selectedRegionIndex === index ? 'selected' : ''}}"
          bindtap="selectRegion"
          data-index="{{index}}"
        >
          <text class="picker-text">{{item.name}}</text>
          <text wx:if="{{selectedRegionIndex === index}}" class="picker-check">✓</text>
        </view>
      </scroll-view>
    </view>
  </view>

  <!-- 用户反馈组件 -->
  <feedback pageName="customs" />
</view>
